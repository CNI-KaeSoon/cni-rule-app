# App

Tauri frontend initialization is intentionally deferred to Lane A.

This directory is reserved for the future Svelte + TypeScript UI. `src-tauri` is not created in M0 so the Lane A Tauri setup can own that scaffold.
